<div class="flex items-center justify-between rounded-lg border border-gray-800 bg-gray-900 px-4 py-3">
  <div class="flex items-center gap-4">
    <div class="h-2 w-2 rounded-full bg-green-500"></div>
    <span class="text-sm text-gray-300"><%= session.started_at.strftime("%b %d, %Y") %></span>
  </div>
  <div class="flex items-center gap-4">
    <span class="text-sm text-gray-400">
      <%= session.started_at.strftime("%I:%M %p") %> — <%= session.ended_at.strftime("%I:%M %p") %>
    </span>
    <span class="text-sm font-semibold text-white"><%= session.duration_seconds / 60 %>m</span>
  </div>
</div>
