<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "NoMore" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-gray-950 text-gray-100">
    <div class="flex min-h-full items-center justify-center px-4 py-12">
      <div class="w-full max-w-md space-y-8">
        <div class="text-center">
          <h1 class="text-4xl font-bold text-red-500 tracking-tight">NoMore</h1>
          <p class="mt-2 text-sm text-gray-400">Stop procrastinating. Start committing.</p>
        </div>

        <% if flash[:alert] %>
          <div class="rounded-lg bg-red-900/50 border border-red-700 p-4 text-sm text-red-300">
            <%= flash[:alert] %>
          </div>
        <% end %>

        <% if flash[:notice] %>
          <div class="rounded-lg bg-green-900/50 border border-green-700 p-4 text-sm text-green-300">
            <%= flash[:notice] %>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
