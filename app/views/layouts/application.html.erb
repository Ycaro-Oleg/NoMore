<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "NoMore" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="NoMore">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-gray-950 text-gray-100">
    <% if current_user %>
      <nav class="border-b border-gray-800 bg-gray-900">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 items-center justify-between">
            <div class="flex items-center gap-8">
              <%= link_to "NoMore", root_path, class: "text-xl font-bold text-red-500 tracking-tight" %>
              <div class="flex gap-6">
                <%= link_to "Dashboard", root_path, class: "text-sm text-gray-300 hover:text-white transition" %>
                <%= link_to "Focus", focus_sessions_path, class: "text-sm text-gray-300 hover:text-white transition" %>
              </div>
            </div>
            <div class="flex items-center gap-4">
              <span class="text-sm text-gray-400"><%= current_user.email %></span>
              <%= button_to "Logout", logout_path, method: :delete, class: "text-sm text-gray-400 hover:text-red-400 transition cursor-pointer" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <% if flash[:notice] %>
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
        <div class="rounded-lg bg-green-900/50 border border-green-700 p-4 text-sm text-green-300">
          <%= flash[:notice] %>
        </div>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
        <div class="rounded-lg bg-red-900/50 border border-red-700 p-4 text-sm text-red-300">
          <%= flash[:alert] %>
        </div>
      </div>
    <% end %>

    <main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
  </body>
</html>
